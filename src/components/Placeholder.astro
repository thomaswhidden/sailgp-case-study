---
interface Props {
  message?: string;
}
const { message = 'Content to be added.' } = Astro.props;
---
<div class="border border-dashed border-zinc-300 rounded px-6 py-10 text-center text-zinc-400">
  <p class="text-sm font-mono">{message}</p>
</div>
